# 安全说明

当前维护范围为 `main` 分支。项目尚未经过第三方安全审计，没有漏洞响应时间承诺。

## 报告问题

若仓库 Security 页面提供 **Report a vulnerability**，请通过该入口私下报告，附复现步骤和影响范围，不附真实用户数据或可用密钥。若该入口尚未启用，请仅开一个不含漏洞细节的 Issue 请求私密联系渠道，等待维护者回复。

## 配置边界

- `.env`、私钥、数据库、照片、备份与本机日志不进入 Git 或镜像。
- 高德 JS Key 必须在浏览器可见，使用域名限制；模型 Key、Web 服务 Key、JS 安全密钥仅后端使用。
- 生产使用 HTTPS 和 Secure Cookie。只有 Caddy 发布端口，数据库与队列只在容器网络内通信。
- `console` 邮件后端不发送邮件。生产接入 SMTP，`EMAIL_DUMP_PATH` 留空。
- 截图和诊断文件也可能含邮箱、Cookie、访问令牌或私人旅行；发布前检查内容和元数据。

`scripts/secret_scan.py` 是规则扫描，输出位置与规则名，不打印命中值。它检查待公开文件、CI 跟踪文件或 Git 暂存区，不能证明所有形式的敏感信息都已发现，也不扫描远端历史或图片文字。发布前仍需人工检查截图和 Git 历史。

如果凭据曾出现在公开提交、Issue、构建日志或公开前端配置中，先撤销并更换凭据，再清理历史与缓存。删除当前文件不等于撤回已公开的凭据。改写历史前保留备份并通知协作者。
